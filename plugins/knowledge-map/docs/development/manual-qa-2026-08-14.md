# Manual QA — 2026-08-14

Environment:

- Obsidian 1.13.7
- Excalidraw 2.26.4
- Windows test vault: `测试`
- Knowledge Map 0.1.0

## Plugin coexistence

- Knowledge Map loaded from `.obsidian/plugins/knowledge-map`.
- Excalidraw loaded from `.obsidian/plugins/obsidian-excalidraw-plugin`.
- `window.ExcalidrawAutomate` was available.
- All four Knowledge Map commands and both custom views were registered.

## Folder map

- The root map rendered without a visible Vault node.
- Five root folders rendered after Excalidraw test drawings were added.
- Both in-canvas node-size and link-thickness sliders rendered.
- Visible `Globe` and `Canvases` toolbar buttons rendered and opened the current folder in their target UI.
- Reusing an already-open 2D or globe tab focused that tab instead of updating it in the background.
- Opening `生活` showed its current-folder node and four Markdown notes without a synthetic `..` parent node.
- The current folder connected to all four direct notes with containment edges.
- A previously untouched `Excalidraw` folder map rendered the current folder at `y = -170` and its two notes in a centered row at `y = 70`.
- Direct children used stable folder-first, natural label ordering.
- Long visual labels were truncated without overlap, while SVG titles retained the complete names.
- A `ResizeObserver` kept the viewport centered after a map was first rendered in a hidden tab.
- Existing user-fixed node positions remained unchanged.

## Canvas management and Excalidraw

- The management modal showed blank, current-folder-map, and globe canvas actions.
- Three dedicated ribbon entries opened the 2D map, globe, and canvas manager.
- Clear command-palette entries for the same actions were registered.
- A blank native Excalidraw drawing was created and opened successfully.
- A root-map Excalidraw drawing was created with editable ellipse and text elements.
- Generated folder elements contained Vault links.
- No error notices appeared.

Generated test drawings were intentionally retained in the test vault's `Excalidraw` folder for visual inspection.

## Globe

- The Three.js canvas created a valid WebGL context.
- All five root labels were visible and readable on first open.
- Fallback globe positions stayed on the initially visible hemisphere.
- Folder labels used a higher-contrast warm style against the globe texture.
- Dragging the `小知识` label persisted new latitude and longitude values.
- Clicking `生活` drilled into its folder globe.
- The drilled globe displayed four note entries without a synthetic parent entry.
- The title rendered as `Vault · Globe` without encoding artifacts.
- No error notices appeared.

## Automated checks

- TypeScript: passed.
- ESLint: passed.
- Vitest: 7 files and 17 tests passed.
- Production esbuild bundle: passed.
